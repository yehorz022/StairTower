import SimulationObject from "./SimulationObject";

export default class TreadStair extends SimulationObject {
  constructor() {
    super();
  }

  draw(
    stair_width: number,
    stair_going: number,
    stair_height: number,
    color: string,
    bottom_color: string
  ) {
  }
}
